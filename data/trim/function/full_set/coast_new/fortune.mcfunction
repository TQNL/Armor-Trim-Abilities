say activated
advancement revoke @s only trim:trim_abilities/mining_away
# fortune only works when mining, so dont have to worry about other sources that change item durability

# unbreaking destroys this method, so cripple default unbreaking enchant and emulate its effect by adding durability back with the correct changes
## give a warning that this is how the pack works

# fix /say commands and 99 level enchant modifier
